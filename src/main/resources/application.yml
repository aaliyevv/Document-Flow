spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/documentflow}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:12345678}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false  # db access is only allowed in service layer, not controller; better performance
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: alialiyev20035558@gmail.com
    password: nujjrxliyxkggxgl
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

server:
  port: 8080

jwt:
  secret: f8K2mP9xL4qT7vN3zR6wY1uD5sA8bC0eX9nM2kJ7 # more than 256 bits for HS256
  expiration: 86400000

kind: Application
